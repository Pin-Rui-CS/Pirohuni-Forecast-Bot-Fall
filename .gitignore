# Secrets
.env
.env.*
!.env.example

# Python cache/build
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtualenvs
.venv/
venv/
env/

# Local generated outputs
forecast_types/
input_questions/
question_types/
test_results/
docs/
data/
sounds/

# Bot run logs and scratch question dumps at repo root
bot_run.log
*_What_will_*.txt

# Run folders downloaded from CI artifacts into the repo root (e.g. 2026-09-14_16-16/)
/[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]_[0-9][0-9]-[0-9][0-9]/

# Local forecast post-mortem / analysis folders (not committed): any
# <question id>_<title> run folder copied to the repo root.
/[0-9][0-9][0-9][0-9][0-9]_*/

# Feedback-loop architecture rebuild — work in progress, not committed yet
# (standalone package; nothing in the main pipeline imports it)
feedback_loop/

# Scratch test scripts at the repo root
test_serp_research.py
test_adapter.py
test_manifold_market_api.py
prompt_preview.py

# Not part of the bot: tests, evaluation tools and working documentation stay local.
tests/
# eval_tools/ is local except the scripts the workflows run (publish_runs.py in
# every forecast workflow, score_outcomes.py in score-outcomes.yaml) and the two
# modules they import.
eval_tools/*
!eval_tools/publish_runs.py
!eval_tools/score_outcomes.py
!eval_tools/score_forecasts.py
!eval_tools/supabase_rest.py
CAPABILITIES.md
INCIDENTS.md
SOCLAAS.md
task-breakdown-44879-44880.md

# Claude Code local config
.claude/

# Handoff doc for the separate reader/website repo (copy it over, then delete)
FORECAST-LIBRARY-HANDOFF.md
